apply plugin: 'java'
apply plugin: 'eclipse'

version = '1.0'

sourceSets {
    main {
        java {
          srcDir projectDir   
        }
    }
}

uploadArchives {
    repositories {
       flatDir {
           dirs 'jar'
       }
    }
}